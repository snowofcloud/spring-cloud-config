spring:
   profiles:
      active:
        - dev
---
server:
  port: 8201

spring:
  profiles: dev
  application:
      name: spring-cloud-config-test-client4

eureka:
 client:
  serviceUrl:
    defaultZone: http://adminn:enjoy@localhost:7001/eureka
  register-with-eureka: false
 
info:
  app.name: spring-cloud-config-client-devdev
  company.name: enjoy
 
---
server:
  port: 8102

spring:
  profiles: beta
  application:
      name: spring-cloud-config-test-client2

eureka:
 client:
  serviceUrl:
    defaultZone: http://adminn:enjoy@localhost:7001/eureka
  register-with-eureka: false
   
info:
  app.name: spring-cloud-config-client-beta
  company.name: enjoybeta
  
  
  
